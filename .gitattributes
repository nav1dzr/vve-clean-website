# The pricing sync script emits LF. Keep its generated mirror byte-stable on
# Windows so a clean test/build does not create a false source change.
admin/api/_lib/pricingCatalogue.generated.js text eol=lf
