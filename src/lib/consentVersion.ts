// Bump this when the cookie categories or their descriptions change in the
// Privacy Policy, so previously-recorded consent is treated as stale and the
// banner can re-prompt. Mirrors the pattern used by termsVersion.ts.
export const CONSENT_VERSION = '2026-07-14';
